@using (Html.BeginForm("Accept", "Requests", new { area = "Employee" }, FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
            {
    <div class="center-block">
        <div class="form-group">
            <input type="submit" class="btn btn-default" value="Accept" />
        </div>
    </div>
}